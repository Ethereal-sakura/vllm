# 示例

vLLM 的示例分为三类：

- 如果你在 Python 代码中使用 vLLM，请参考 *离线推理（Offline Inference）* 部分。
- 如果你通过 HTTP 应用或客户端使用 vLLM，请参考 *在线服务（Online Serving）* 部分。
- 如果你想了解 vLLM 的一些高级功能（如 LMCache 或 Tensorizer）的使用示例，这些功能并不特定于上述两种场景，请参考 *其他（Others）* 部分。